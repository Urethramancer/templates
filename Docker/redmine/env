REDMINE_DB_POSTGRES=postgres-red
REDMINE_DB_NAME=redmine
REDMINE_DB_USERNAME=redmine
REDMINE_DB_PASSWORD=redmine

SMTP_ENABLED=false
SMTP_METHOD=smtp
SMTP_DOMAIN={SMTP_DOMAIN}
SMTP_HOST={SMTP_HOST}
SMTP_PORT={SMTP_PORT}
SMTP_USER={SMTP_USER}
SMTP_PASS={SMTP_PASSWORD}
SMTP_STARTTLS=true
SMTP_AUTHENTICATION=:login

IMAP_ENABLED=false
IMAP_HOST={IMAP_HOST}
IMAP_PORT={IMAP_PORT}
IMAP_USER={IMAP_USER}
IMAP_PASS={IMAP_PASSWORD}
IMAP_SSL=true
IMAP_INTERVAL=30
